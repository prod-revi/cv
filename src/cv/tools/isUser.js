import React from 'react'

const isUser = () => {
  // tiene nombre?
    // para esto necesitamos de React Router
    // 

  // si no tiene nombre?
  // chupame las pelotas
  // mentiras
  // un default
  // crea tu propia cv
  // llamar a la base de datos
  // solamente leer
  // si no tiene nombre entonces tranca default
  // si no hay nombre default
  // y listo entonces corremos ahora si cuando ya tenemos la data
  // procedemos a rellenar los diferentes cubos
  // y asi ya tienen solo la informacion que necesitan
  // entonces cada cubo esta divido entre un input y un texto
  // bueno va a ser lo siguiente

  // entonces 
}

export default isUser